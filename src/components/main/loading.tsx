import * as S from "./style";

const Loading = () => {
    return (
        <S.LoadingNovel>
            <S.LoadingImage />
            <S.LoadingTitle />
            <S.LoadingViews />
        </S.LoadingNovel>
    );
};

export default Loading;